[mutmut]
# Chemins des fichiers à tester (séparés par des virgules, sans espaces)
paths_to_mutate=api,models.py,schemas.py

# Commande de test à exécuter pour chaque mutation  
runner=python -m pytest -x --tb=short

# Dossier de backup
backup_dir=.mutmut_backup

